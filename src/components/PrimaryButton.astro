---
const {label, link} = Astro.props;
const darkMode = Astro.props.darkMode || true;
---

<!--<a href={link} class="btn btn-dark fw-bold mt-3">{label}</a>-->


<a href={link} class={`btn fw-bold mt-3 ${darkMode ? 'btn-dark' : 'btn-outline-light'}`}>{label}</a>