---
const {label, link} = Astro.props;
---

<a href={link} class="btn btn-outline-dark btn-lg fw-bold w-50" style="border-radius: 3px;
    font-weight: 200;
    min-width: 160px;
    font-size: 1.2rem;
   ">{label}</a>
