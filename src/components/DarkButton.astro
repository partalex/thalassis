---
const { label, link } = Astro.props;
---

<style>
    .a {
        font-weight: 300;
        max-width: 200px !important;
        width: 150px !important;
    }
    /* If you want animation for button just uncomment this section */
    /* 
    .offset-shadow {
        color: white;
        background: black;
        box-shadow: 0.3em 0.3em 0 #808080;
        border: 1px solid #979695;

        transition:
            box-shadow 0.3s ease,
            transform 0.3s ease,
            background 0.3s ease,
            color 0.3s ease;
        }

.offset-shadow:hover {
  box-shadow: -0.3em -0.3em 0 #979695;
  background: #808080;
  color: black;
  transform: translate(-0.3em, -0.3em);
} */
</style>

<a
    href={link}
    class="btn btn-dark btn-lg black-btn offset-shadow"
    style="border-radius: 3px;
    font-weight: 200;
    min-width: 160px;
    min-height: 40px;
    font-size: 1rem;
   "
    >{label}</a
>
