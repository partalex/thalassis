---
const {label, link} = Astro.props;
---

<!--<a href={link} class="btn btn-outline-dark fw-bold mt-3">{label}</a>-->
<a href={link} class="btn btn-outline-dark" style="border-radius: 3px;
    font-weight: 200;
    min-width: 160px;
    font-size: 1.2rem;
   ">{label}</a>

